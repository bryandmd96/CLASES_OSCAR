version "3.9"

services:
  api:
    build:
    container_name: rest_api_node
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumenes.
      - .:/usr/src/app
      - /usr/src/app_node_modules
    
    command: npm run dev

  